const router = require('express').Router();
const db = require('../db/index');

router.get('/playerslist', (req, res) => {});
router.post('/submit-player', (req, res) => {});

module.exports = router;